# ===== Python =====
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.Python

# ===== Model ağırlıkları =====
# TEK İSTİSNA models/best.pt: takımın ortak modeli, herkes AYNI ağırlıkla çalışsın diye
# repoda durur (18 MB, GitHub sınırlarının çok altında). Türetilmiş biçimler
# (.onnx / .engine) GİRMEZ — .engine TensorRT çıktısıdır, GPU modeline + TensorRT
# sürümüne + sürücüye bağlıdır, başka makinede AÇILMAZ. Herkes best.pt'den kendisi üretir:
#     yolo export model=models/best.pt format=engine   (veya format=openvino)
*.pt
*.onnx
*.engine
!models/best.pt
# models/ klasörü YAPISI repoda kalır (README.md + .gitkeep hariç tutulur):
!models/README.md
!models/.gitkeep

# ===== Veri setleri / eğitim çıktıları (repoya girmez) =====
*.zip
datasets/
dataset/
dataset_v*/
runs/
roboflow_rf/

# NOT: Dokümanlar repoda AÇIKTIR — yalnızca 2 dosya: CLAUDE.md (proje beyni) + README.md.

# ===== Gizli yarışma belgeleri (repoya asla girmemeli) =====
Analiz/
Modeller_Kil6t/
*.pdf
*.3mf
# İSTİSNA: Şartname resmi/kamuya açık bir belge — repoda tutulur (şartname taraması için).
# KTR raporu, parkur çizimi, 3mf maketler yine repoya GİRMEZ (yukarıdaki kurallar geçerli).
!sartname/
!sartname/*.pdf
!sartname/*.txt

# ===== Arayüz ayarları =====
# app/ayarlar.json artık REPODA (takım kararı 08.08): herkes aynı kalibrasyonla
# başlasın diye. Makineye özel hiçbir şey içermez (yol/kamera indeksi yok, yalnız
# eşik ve PD katsayıları). Kendi denemeni yaparken dosyayı değiştirmen normaldir —
# ortak değere dönmek için: git checkout app/ayarlar.json

# ===== IDE / OS =====
.vscode/
.idea/
Thumbs.db
Desktop.ini
.DS_Store
