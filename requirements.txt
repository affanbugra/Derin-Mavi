# DERİN MAVİ — Görev Kontrol İstasyonu bağımlılıkları
# Kurulum:  pip install -r requirements.txt
#
# Not: ultralytics kendi torch sürümünü çeker. GPU'suz makinelerde CPU torch yeterlidir
# (canlı tespit CPU'da çalışır). Küçük model (yolov8n/11n) + imgsz 640 önerilir.

ultralytics>=8.1.0
opencv-python>=4.8.0
numpy>=1.24.0
PySide6>=6.6.0

# Gerçek ESP32 ile seri haberleşme (kontrol.py). Mock ile çalışırken gerekmez ama
# donanım gelince ŞART — bu yüzden zorunlu bağımlılık, "bende çalışıyordu" olmasın.
pyserial>=3.5

# USB gamepad ile manuel kontrol (app/gamepad.py). Şartname Yetenek 1 kullanıcı komut
# arayüzlerini "UI/joystick/klavye" diye sayar — joystick videoda gösterilecek.
# SDL sayesinde hem XInput hem DirectInput padleri okur. Kurulu değilse uygulama yine
# açılır, yalnızca gamepad özelliği kapalı kalır.
pygame>=2.5

# --- Opsiyonel: CPU'da hızlı çıkarım ---
# Bu proje GPU'suz laptopta koşuyor ve tek gerçek darboğaz CPU inference'ı.
# OpenVINO Intel işlemcide tipik 2-3× hızlandırır. Kurup modeli bir kez çevirin:
#     pip install openvino
#     yolo export model=models/best.pt format=openvino
# Çıktı models/best_openvino_model/ olarak kalır; uygulama onu OTOMATİK tercih eder.
# openvino>=2024.0
#
# ONNX alternatifi (DERINMAVI_MODEL=onnx):
# onnxruntime>=1.16.0
